/*
* start React: npm start
*
* Install plugins: go to npmjs.com
* Axios - helps to send a request and get a response (async.(asynchronous) requests)
* PS D:\js\training\jane> npm i axios
*
* There are 4 request methods:
* - get (default request);
* - post (send data to server to create sth (user/product/page);
* - put (update);
* - delete (delete sth).
*
* */




if(name === 'Max') {

} else {

}

name === 'Max' ? 'Max' : 'Jane'

const names = ['Max', 'Jane']

// const boy = names[0]
// const girl = names[1]

const [hus, girl] = names

const jane = {
	name: 'jane',
	age: 18
}

const name = jane.name
const age = jane.age

const {name, age} = jane